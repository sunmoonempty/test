#include <bits/stdc++.h>

char s[10005];
int main(){
    int T;
    scanf("%d",&T);
    while (T--)
    {
        scanf("%s",s);
        int len=strlen(s);









        memset(s,'\0',sizeof(s));
    }
    
    return 0;
}